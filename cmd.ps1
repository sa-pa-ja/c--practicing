Write-Output "git-init create an empty GIT repository or reinitialize an existing one"
git init && git add -A

Write-Host "git init && git add -A && git commit -m "...""

Write-Host "git branch -M main && git remote add origin Url && git pull origin main --rebase && git push -u origin main "


